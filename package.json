{
  "name": "kitsune-download-manager",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "install:app": "npm install --prefix app",
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app",
    "build:rust": "npm run build:rust --prefix app",
    "dist:linux": "npm run dist:linux --prefix app",
    "dist:win": "npm run dist:win --prefix app"
  }
}
